# Environment variables for gym equipment logger

# Storage mode: 'localStorage' or 'file'
# - localStorage: Store data in browser localStorage (default)
# - file: Store data in JSON files via backend API
VITE_STORAGE_MODE=file

# API base URL (when using file storage)
VITE_API_BASE_URL=http://localhost:3001
